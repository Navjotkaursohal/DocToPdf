// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // apiUrl: 'http://localhost:8000'
  // apiUrl: 'http://2bofficedubai.ddns.net:7445'
  // apiUrl: 'http://192.168.18.50:8000'
  apiUrl: 'http://192.168.3.76:8000'

  // apiUrl: 'https://dewaserv9794be.dewa.gov.ae'

  // apiUrl: 'http://dewaserv9794.dewa.gov.ae',

  // apiUrl: 'https://dewaserv1614.dewa.gov.ae'

  // apiUrl: 'https://ad.deskalert.local/Alerts',
  // apiUrl: 'http://172.30.11.20:80/v1/sysadmin'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
